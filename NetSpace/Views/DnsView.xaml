<UserControl x:Class="NetSpace.Views.DnsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NetSpace.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Border CornerRadius="15"  Background="#1b314c">
            <Grid >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>

                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>

                </Grid.RowDefinitions>
                 <ComboBox SelectedItem="{Binding SelectedDnsType}" ItemsSource="{Binding DnsTypes}" Style="{DynamicResource CustomComboBox}">
                </ComboBox>

                <Grid Grid.Row="1" IsEnabled="{Binding HandleEdit}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="0.9*" />
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="*"/>

                    </Grid.RowDefinitions>
                    <Border Style="{DynamicResource RadioBack}">
                        <CheckBox Content="IPv4" Style="{DynamicResource IpSwitcher}" IsChecked="{Binding SwitchIpv4}"/>
                    </Border>


                    <Border Grid.Column="1" Style="{DynamicResource RadioBack}">
                        <CheckBox Content="IPv6" Style="{DynamicResource IpSwitcher}" IsChecked="{Binding SwitchIpv6}"/>
                    </Border>

                    <Border Grid.Row="1" Grid.Column="1" IsEnabled="{Binding SwitchIpv6}"  Style="{DynamicResource BorderApp}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                            </Grid.RowDefinitions>
                            <Border HorizontalAlignment="Center" Padding="15,5,15,5" Grid.Row="0" Style="{DynamicResource BorderApp}" Background="#1b314c">
                                <TextBlock FontSize="14" TextWrapping="WrapWithOverflow" Text="Предпочтительный DNS сервер" Margin="5" Style="{DynamicResource TitleMainApp}"/>
                            </Border>
                            <Border  Padding="15,5,15,5" Grid.Row="1" Style="{DynamicResource BorderApp}" Background="#1b314c">
                                <TextBox TextAlignment="Center"  Text="{Binding DnsSet._ipv6First}" Margin="5" Style="{DynamicResource TextBoxDns}"/>
                            </Border>
                            <Border HorizontalAlignment="Center" Padding="15,5,15,5" Grid.Row="2" Style="{DynamicResource BorderApp}" Background="#1b314c">
                                <TextBlock FontSize="14" TextWrapping="WrapWithOverflow" Text="Дополнительный DNS сервер" Margin="5" Style="{DynamicResource TitleMainApp}"/>
                            </Border>
                            <Border  Padding="15,5,15,5" Grid.Row="3" Style="{DynamicResource BorderApp}" Background="#1b314c">
                                <TextBox TextAlignment="Center"  Text="{Binding DnsSet._ipv6Second}" Margin="5" Style="{DynamicResource TextBoxDns}"/>
                            </Border>
                        </Grid>
                    </Border>
                    <Border Grid.Row="1"  IsEnabled="{Binding SwitchIpv4}" Style="{DynamicResource BorderApp}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                            </Grid.RowDefinitions>
                            <Border HorizontalAlignment="Center" Padding="15,5,15,5" Grid.Row="0" Style="{DynamicResource BorderApp}" Background="#1b314c">
                                <TextBlock FontSize="14" TextWrapping="WrapWithOverflow" Text="Предпочтительный DNS сервер" Margin="5" Style="{DynamicResource TitleMainApp}"/>
                            </Border>
                            <Border  Padding="15,5,15,5" Grid.Row="1" Style="{DynamicResource BorderApp}" Background="#1b314c">
                                <TextBox  TextAlignment="Center"  Text="{Binding DnsSet._ipv4First}" Margin="5" Style="{DynamicResource TextBoxDns}"/>
                            </Border>
                            <Border HorizontalAlignment="Center" Padding="15,5,15,5" Grid.Row="2" Style="{DynamicResource BorderApp}" Background="#1b314c">
                                <TextBlock FontSize="14" TextWrapping="WrapWithOverflow" Text="Дополнительный DNS сервер" Margin="5" Style="{DynamicResource TitleMainApp}"/>
                            </Border>
                            <Border  Padding="15,5,15,5" Grid.Row="3" Style="{DynamicResource BorderApp}" Background="#1b314c">
                                <TextBox TextAlignment="Center"  Text="{Binding DnsSet._ipv4Second}" Margin="5" Style="{DynamicResource TextBoxDns}"/>
                            </Border>
                        </Grid>
                    </Border>

                </Grid>

            </Grid>
        </Border>
        <Button Command="{Binding SaveCommand}" Content="Сохранить изменения" HorizontalAlignment="Center" FontSize="18"  Style="{DynamicResource ToolBarButton}" Grid.Row="1" Margin="10"/>

    </Grid>
</UserControl>
