<UserControl x:Class="NetSpace.Views.OtherView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NetSpace.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
        <Border CornerRadius="15"  Background="#1b314c">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>

            </Grid.RowDefinitions>
            <Button Command="{Binding ClearStatCommand}"  Content="Очистить статистику трафика" HorizontalAlignment="Center" FontSize="20"  Style="{DynamicResource ToolBarButton}" Grid.Row="0" Margin="10"/>
            <TextBlock FontSize="14" TextWrapping="WrapWithOverflow" Text="*Приложение перезапустится" Opacity="0.6" Margin="5" Style="{DynamicResource TitleMainApp}" Grid.Row="1"/>
            <Button Command="{Binding FLushDNSCommand}" Content="Очистить DNS кэш"  HorizontalAlignment="Center" FontSize="20"  Style="{DynamicResource ToolBarButton}" Grid.Row="2" Margin="10"/>
            <Button Command="{Binding RepairCommand}" Content="Исправление проблем с сетью"  HorizontalAlignment="Center" FontSize="20"  Style="{DynamicResource ToolBarButton}" Grid.Row="3" Margin="10"/>
        </Grid>
        </Border>
   
</UserControl>
