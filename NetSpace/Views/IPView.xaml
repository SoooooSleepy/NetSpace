<UserControl x:Class="NetSpace.Views.IPView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NetSpace.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Border CornerRadius="15"  Background="#1b314c">
            <Grid >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>

                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>

                </Grid.RowDefinitions>
                <ComboBox SelectedItem="{Binding SelectedDnsType}" ItemsSource="{Binding DnsTypes}" Style="{DynamicResource CustomComboBox}">
                </ComboBox>

                <Grid Grid.Row="1" IsEnabled="{Binding HandleEdit}">
                    
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="*"/>

                    </Grid.RowDefinitions>
                   

                   
                    <Border Grid.Row="1"   Style="{DynamicResource BorderApp}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                            </Grid.RowDefinitions>
                            <Border HorizontalAlignment="Center" Padding="15,5,15,5" Grid.Column="0" Style="{DynamicResource BorderApp}" Background="#1b314c">
                                <TextBlock FontSize="14" TextWrapping="WrapWithOverflow" Text="IP-адрес" Margin="5" Style="{DynamicResource TitleMainApp}"/>
                            </Border>
                            <Border  Padding="15,5,15,5" Grid.Column="1" Style="{DynamicResource BorderApp}" Background="#1b314c">
                                <TextBox  TextAlignment="Center"  Text="{Binding IPSet._address}" Margin="5" Style="{DynamicResource TextBoxDns}"/>
                            </Border>
                            <Border HorizontalAlignment="Center" Padding="15,5,15,5" Grid.Column="0" Grid.Row="1" Style="{DynamicResource BorderApp}" Background="#1b314c">
                                <TextBlock FontSize="14" TextWrapping="WrapWithOverflow" Text="Маска подсети" Margin="5" Style="{DynamicResource TitleMainApp}"/>
                            </Border>
                            <Border  Padding="15,5,15,5" Grid.Column="1" Grid.Row="1" Style="{DynamicResource BorderApp}" Background="#1b314c">
                                <TextBox TextAlignment="Center"  Text="{Binding  IPSet._mask}" Margin="5" Style="{DynamicResource TextBoxDns}"/>
                            </Border>
                            <Border HorizontalAlignment="Center" Padding="15,5,15,5" Grid.Column="0" Grid.Row="2" Style="{DynamicResource BorderApp}" Background="#1b314c">
                                <TextBlock FontSize="14" TextWrapping="WrapWithOverflow" Text="Шлюз" Margin="5" Style="{DynamicResource TitleMainApp}"/>
                            </Border>
                            <Border  Padding="15,5,15,5" Grid.Column="1" Grid.Row="2" Style="{DynamicResource BorderApp}" Background="#1b314c">
                                <TextBox TextAlignment="Center"  Text="{Binding  IPSet._gateway}" Margin="5" Style="{DynamicResource TextBoxDns}"/>
                            </Border>


                            
                        </Grid>
                    </Border>

                </Grid>

            </Grid>
        </Border>
        <Button Command="{Binding SaveCommand}"  Content="Сохранить изменения" HorizontalAlignment="Center" FontSize="18"  Style="{DynamicResource ToolBarButton}" Grid.Row="1" Margin="10"/>

    </Grid>
</UserControl>
